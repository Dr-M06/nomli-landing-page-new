import React from 'react';
import CreatorMonetizationFlow from '../../components/creator/CreatorMonetizationFlow';

/**
 * Six creator monetisation surfaces (HTML mock parity) with in-flow tab chips.
 * Dark chrome matches `nomli_creator_screens.html`; amounts in USD.
 */
export default function CreatorHubScreen() {
  return <CreatorMonetizationFlow />;
}
