/**
 * Home tab — first tab in the tab bar (labeled "Home").
 * Rebuilt as a focused feed; the previous ~10k-line implementation is kept as
 * `archive/CommunityHomeLegacy.tsx` for reference or rollback.
 */
export { default } from '../../components/home/HomeFeedScreen';
