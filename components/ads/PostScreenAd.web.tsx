import React from 'react';

/** AdMob is native-only; web has no `react-native-google-mobile-ads` implementation. */
export default function PostScreenAd(_props: { isDark?: boolean }) {
  return null;
}
