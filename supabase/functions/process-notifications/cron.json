{
  "cron": "*/2 * * * *",
  "description": "REQUIRED for timely pushes when the sender app cannot invoke this function: process pending notification_queue every 2 minutes. Enable in Supabase Dashboard → Edge Functions → process-notifications → Schedules/Cron. Optional: Database Webhook on notification_queue INSERT → POST this function with body { \"record\": { \"id\": \"...\" } }."
}
